{
  "name": "myapp",
  "version": "1.0.0",
  "description": "MyApp is the first electron app in my learning session",
  "main": "auto_build/main.js",
  "scripts": {
    "compile": "tsc && copyfiles -f src/index.html auto_build",
    "start": "npm run compile && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "al fian",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "electron": "^20.0.2",
    "typescript": "^4.7.4"
  }
}
